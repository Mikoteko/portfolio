Start the structured planning methodology for: $ARGUMENTS

Follow the 4-phase workflow defined in `.claude/rules/20-project-planning-methodology.instructions.md`.

Begin with Phase 1 (Research). Create the working directory at `docs/plans/<task-slug>/` and write `research.md`. Do NOT write any implementation code — research only.
